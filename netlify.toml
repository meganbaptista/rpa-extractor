[functions]
  node_bundler = "esbuild"
  external_node_modules = ["pdfjs-dist"]

# Disclosure Intake Pipeline — the watcher runs every 10 minutes, scans each
# "Incoming" folder, and emits a disclosure.uploaded event per new PDF.
[functions."disclosure-watch-poller"]
  schedule = "*/10 * * * *"
